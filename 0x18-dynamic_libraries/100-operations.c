/**
 * add - addition of two integers
 * @a: first number
 * @b: second number
 * Return: result
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - subtraction of integers
 * @a: first number
 * @b: second number
 * Return: result
 */
int sub(int a, int b)
{
	return (a - b);
}

/**
 * mul - multiplication
 * @a: first number
 * @b: second number
 * Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 * div - division
 * @a: first number
 * @b: second number
 * Return: result
 */
int div(int a, int b)
{
	return (a / b);
}

/**
 * mod - modulus of two integers
 * @a: first number
 * @b: second number
 * Return: result
 */
int mod(int a, int b)
{
	return (a % b);
}
